# Dipendenze del worker RunPod. torch/CUDA arrivano dall'immagine base,
# flash-attn viene compilato a parte nel Dockerfile (--no-build-isolation).
nano-vllm-voxcpm
runpod>=1.6.0
numpy>=1.24
boto3>=1.34
huggingface_hub>=0.23
hf_transfer>=0.1.6
